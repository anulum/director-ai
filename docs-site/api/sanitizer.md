# Input Sanitizer API

Input sanitization for prompt/response pairs before scoring. Strips injection patterns,
normalizes whitespace, and enforces length limits.

::: director_ai.core.sanitizer.InputSanitizer
