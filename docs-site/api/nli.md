# NLI API

Natural Language Inference scorer backends (DeBERTa, ONNX, MiniCheck).
`NLIScorer` is used internally by `CoherenceScorer` — direct use is only needed for custom pipelines.

::: director_ai.core.nli.NLIScorer

::: director_ai.core.nli.nli_available
