# Independent Deep Audit — Director-Class AI v3.9.4

## Context

You are performing an independent code audit of **Director-Class AI**, a real-time LLM hallucination guardrail system. The package name is `director-ai` (PyPI). The product name is "Director-Class AI".

**Repo**: `C:\aaa_God_of_the_Math_Collection\03_CODE\DIRECTOR_AI`
**Branch**: main @ 7cee39f
**Version**: 3.9.4
**Language**: Python (94 source files, 162 test files, 62 docs-site pages, 16 Jupyter notebooks, 18 root docs)

A prior audit (by Claude) found and fixed 43 issues across 28 files. We need you to verify that audit's work AND find anything it missed. The prior audit had a 34% false positive rate — do not trust its claims; verify everything yourself.

## Your Mission

Perform a deep, systematic audit covering these 7 dimensions. For EVERY finding, you must cite the exact file and line number, and quote the relevant code. No vague claims.

---

### DIMENSION 1: Scoring Math Correctness

Read `src/director_ai/core/scoring/scorer.py` (the main scorer, ~1530 lines) end-to-end.

Verify:
- **Weight constants** `W_LOGIC` and `W_FACT`: do they always sum to 1.0? Is this enforced at construction time AND in all config profiles?
- **Coherence formula**: `coherence = 1.0 - (W_LOGIC * h_logic + W_FACT * h_fact)`. Can `coherence` ever go outside [0, 1]? Trace every path where h_logic and h_fact are computed.
- **No-KB calibration** (around line 1279-1288): the rescaling formula `(coherence - lo) / span` maps from `[lo, hi]` to `[0, 1]`. Verify the theoretical bounds `lo = 1 - W_LOGIC - W_FACT * 0.5` and `hi = 1 - W_FACT * 0.5` are mathematically correct. Check the guard against `span <= 1e-9`.
- **Cross-turn blending** (around line 1425): `h_logic = 0.7 * h_logic + 0.3 * cross_turn`. Verify this produces values in [0, 1] and that re-calibration is correctly applied after blending.
- **Dialogue scoring path** (lines ~1220-1250): when `_use_prompt_as_premise` is False and dialogue is detected, `h_logic = 0.0`. Verify the implication: coherence floor is `1.0 - W_FACT * h_fact`, never below `1.0 - W_FACT`. At default weights, this means coherence >= 0.6.
- **`_should_escalate()`** (line ~427): accepts `task_type` param but all 4 call sites (lines ~826, ~906, ~956, ~1039) pass only the score. Confirm this is dead code.
- **Meta-classifier threshold mapping** (line ~1473): recently changed from `0.4 + 0.6 * nli_threshold` to `self.W_FACT + self.W_LOGIC * nli_threshold`. Verify this is mathematically correct.
- **Summarization path** (`_summarization_factual_divergence`): verify the bidirectional NLI formula and baseline calibration.

Read `src/director_ai/core/scoring/nli.py`:
- **Label index resolution** (`_resolve_label_indices`): verify it correctly handles models where contradiction is NOT at index 2.
- **Softmax** (`_softmax_np`): verify numerical stability (max subtraction).
- **3-class divergence**: `P(contradiction) + 0.5 * P(neutral)`. Verify range is [0, 1].
- **Confidence computation**: verify normalized entropy `1 - H(p)/log(K)` is correct.
- **`score_claim_coverage`**: verify return type is `tuple[float, list[float], list[str]]`.

Read `src/director_ai/core/scoring/lite_scorer.py`:
- Verify `score()` returns divergence in [0, 1].
- Note any interface mismatches with `CoherenceScorer.review()`.

---

### DIMENSION 2: Wiring & Functionality

Trace the complete data flow from user entry points to scoring:

1. **`guard()` in `integrations/sdk_guard.py`**: trace how it creates a `CoherenceScorer`, wraps the SDK client, and intercepts calls. Verify the 5 SDK shapes (OpenAI, Anthropic, Bedrock, Gemini, Cohere) each correctly extract prompt/response from the SDK's response format.

2. **`score()` in `integrations/sdk_guard.py`**: verify it creates a scorer, calls `scorer.review()`, and returns a `CoherenceScore`.

3. **`__init__.py` exports**: read `src/director_ai/__init__.py` and `PUBLIC_API.md`. For every symbol in `__all__`, verify it traces to an actual class/function in the source. Check for any missing symbols.

4. **Config → Scorer wiring**: read `core/config.py`, find `build_scorer()` method. Verify every config field is correctly transferred to the `CoherenceScorer` constructor. Particularly verify weight overrides, device settings, cache, and profile-specific fields.

5. **Threshold defaults**: document EVERY place a default threshold is set. Known: `guard()` = 0.3, `score()` = 0.3, `CoherenceScorer.__init__` = 0.5, `DirectorConfig.coherence_threshold` = 0.6, all framework integrations = 0.6. Verify these are accurate.

6. **BatchProcessor** (`core/runtime/batch.py`): verify `review_batch()` on `CoherenceScorer` is sequential (list comprehension). Verify `BatchProcessor` correctly detects native batch support and falls back.

7. **Streaming** (`core/runtime/streaming.py`): verify the 3 halt mechanisms (hard_limit, window_avg, trend_drop) are all wired. Verify the `coherence_callback` receives accumulated text, not individual tokens.

---

### DIMENSION 3: Documentation vs Code

For EVERY docs-site page (`docs-site/api/*.md`, `docs-site/guide/*.md`, `docs-site/quickstart.md`):

- Read the page
- For every function signature shown: verify parameter names, types, defaults, and return types against the actual source code
- For every code example: verify imports resolve, function calls use correct params
- For every claim about behavior: verify against source code

Pay special attention to these recently-fixed pages (verify the fixes are correct):
- `docs-site/api/agent.md` — constructor params, `stream()` method
- `docs-site/api/batch.md` — `BatchResult` fields, constructor param name
- `docs-site/api/guard.md` — threshold defaults (should be 0.3)
- `docs-site/api/types.md` — `CoherenceScore` fields (no `claim_attributions`)
- `docs-site/api/scorer.md` — `review()` second param is `action` not `response`
- `docs-site/api/streaming.md` — `window_threshold` default 0.55, `soft_limit` default 0.6
- `docs-site/api/cache.md` — `ScoreCache.get/put/invalidate` signatures
- `docs-site/api/vector-store.md` — `get_vector_backend()` returns class not instance

Also check pages that were NOT modified — they may still contain errors:
- `docs-site/guide/streaming.md`
- `docs-site/guide/streaming-overhead.md`
- `docs-site/guide/architecture.md`
- `docs-site/guide/troubleshooting.md`
- `docs-site/guide/migration-v2-v3.md`
- `docs-site/deployment/runbooks.md`
- `docs-site/tutorials.md`
- `docs-site/guide/error-handling.md`
- `docs-site/guide/why-director-ai.md`
- `docs-site/guide/rust-ffi.md`
- `docs-site/guide/presets.md`
- `docs-site/guide/scoring.md`

---

### DIMENSION 4: Notebooks

Read all 16 notebooks in `notebooks/`. For each:

1. Verify every `from director_ai import X` statement — does X exist in `__init__.py`?
2. Verify every function call uses correct parameter names and types
3. Verify markdown claims are accurate (version numbers, performance numbers, feature descriptions)
4. Check for encoding issues (invalid UTF-8 bytes)
5. Check cell execution order — are variables used before definition?

Recently fixed notebooks (verify fixes are correct): 01, 03, 11, 12.
Unmodified notebooks that need full audit: 02, 04, 05, 06, 07, 08, 09, 10, 13, 14, 15, quickstart.

---

### DIMENSION 5: README, CHANGELOG, ROADMAP, CONTRIBUTING, CITATION.cff, pyproject.toml

- **README.md**: every performance claim must cite a specific benchmark. Every feature must exist in code. Every code example must be runnable. The "Known Limitations" section must reflect current reality.
- **CHANGELOG.md**: v3.9.3 and v3.9.4 entries must accurately describe shipped changes.
- **ROADMAP.md**: every item marked "Done" must have corresponding code. Every item marked "Deferred" must NOT have production code. Version ordering must be reverse-chronological within major versions.
- **CONTRIBUTING.md**: architecture table must match actual `src/director_ai/core/` subpackage structure (scoring/, runtime/, retrieval/, safety/).
- **CITATION.cff**: version 3.9.4, leaderboard position #8 among 19 systems. Product name "Director-Class AI".
- **pyproject.toml**: version 3.9.4, entry points correct, all extras non-empty or documented as placeholders.

---

### DIMENSION 6: Test Suite Quality

Do NOT run tests. Instead, read test files and evaluate:

1. **Scoring tests** (`tests/test_coverage_scorer*.py`, `tests/test_dialogue_fpr.py`, `tests/test_summarization_bidir.py`, `tests/test_no_kb_calibration.py`): do assertions verify actual values, or just types/non-null?
2. **Guard tests** (`tests/test_sdk_guard.py`, `tests/test_cov_sdk_guard.py`): are all 5 SDK shapes tested?
3. **Streaming tests** (`tests/test_streaming.py`): are halt mechanisms tested with known score sequences?
4. **Anti-patterns**: find tests that mock away the thing they're testing, assertions that always pass (`assert True`, `assert isinstance(x, object)`), tests with no assertions.
5. **Coverage gaps**: identify critical code paths with no test coverage.

---

### DIMENSION 7: Security & Correctness

1. **Input validation**: check all user-facing entry points (guard, score, review, CLI, server endpoints) for missing input validation.
2. **API key handling**: verify no API keys are logged, cached in plaintext, or included in error messages.
3. **Serialization**: check `pickle`, `yaml.load`, `eval`, `exec` usage.
4. **Dependency injection**: verify `ground_truth_store`, `nli_model`, `scorer_backend` parameters are validated.
5. **Thread safety**: `ScoreCache`, `BatchProcessor`, `ConversationSession` — verify lock usage is correct.

---

## Output Format

For each finding, use this exact format:

```
[SEVERITY] FILE:LINE — Description
  EVIDENCE: <quoted code or text>
  SHOULD BE: <corrected version>
  CATEGORY: math-error | logic-error | wrong-signature | wrong-default | stale-claim | dead-code | security | test-quality | missing-validation
```

Severities: CRITICAL (crashes/wrong results), HIGH (confuses users), MEDIUM (inaccurate), LOW (cosmetic).

At the end, produce a summary table:
```
| Severity | Count | Categories |
|----------|-------|------------|
| CRITICAL | N     | ...        |
| HIGH     | N     | ...        |
| MEDIUM   | N     | ...        |
| LOW      | N     | ...        |
```

## Rules

1. **No false positives.** Every finding must include the exact line number and quoted evidence. If you're not sure, say "UNVERIFIED" and explain what you couldn't confirm.
2. **No aspirational language.** Report what IS, not what should be.
3. **Check the recent fixes.** 28 files were modified in the last 6 commits. Verify those changes are correct — don't assume they are just because they're recent.
4. **The product name is "Director-Class AI"** (not "Director-AI"). The package/repo is `director-ai`.
5. **Read the actual source code.** Do not guess signatures, defaults, or return types from documentation. Read the `.py` files.
