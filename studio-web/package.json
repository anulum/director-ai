{
  "name": "@anulum/director-ai-studio",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Director-AI AI-Safety / Streaming Academy — dedicated Tier-B portal (Phase-0 scaffold)",
  "license": "Apache-2.0",
  "scripts": {
    "sync:manifest": "node scripts/sync-manifest.mjs",
    "predev": "npm run sync:manifest",
    "prebuild": "npm run sync:manifest",
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "e2e": "playwright test",
    "lint": "eslint . && tsc --noEmit"
  },
  "dependencies": {
    "react": "19.0.0",
    "react-dom": "19.0.0",
    "react-router-dom": "7.1.1",
    "zustand": "5.0.2",
    "backfire-wasm": "file:../backfire-kernel/crates/backfire-wasm/pkg"
  },
  "peerDependencies": {
    "@anulum/ui": "workspace:*",
    "@anulum/portal-kit": "workspace:*"
  },
  "devDependencies": {
    "@module-federation/vite": "1.4.1",
    "@playwright/test": "1.49.1",
    "@types/react": "19.0.2",
    "@types/react-dom": "19.0.2",
    "@vitejs/plugin-react": "4.3.4",
    "@vitest/coverage-v8": "2.1.8",
    "eslint": "9.17.0",
    "jsdom": "25.0.1",
    "typescript": "5.8.2",
    "vite": "8.0.16",
    "vitest": "3.2.6"
  },
  "comment": "@anulum/ui + @anulum/portal-kit are peer deps published by the SCPN-STUDIO keeper; until then a local Instrument-Deck token mirror (src/tokens) + local honesty bridge (src/contract/honesty.ts) stand in. Pinned versions are placeholders to be reconciled against the workspace lock at integration."
}
