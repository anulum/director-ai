# Director-AI Session Handover — 2026-03-31

## Completed

### 1. Deploy Judge — PyPI + Docker + GHCR
- **PyPI**: v3.11.1 already published (confirmed via `pip index versions`)
- **GHCR**: v3.11.1 CPU + GPU images already published (confirmed via `docker manifest inspect`)
- **No version bump needed** — judge was already part of v3.11.1

### 2. SPDX Header Compliance Fix
- **Commit**: `7f6780b` + `575e66f` + `a1a560f`
- **509 files**: split combined `AGPL-3.0-or-later | Commercial` → two lines
- **18 files**: `Sotek` → `Šotek` (háček)
- **19 files**: `(C)` → `©` in shell scripts
- **13 files**: added missing SPDX headers (YAML, docs, configs)
- **2 files** skipped: `director_pb2.py`, `director_pb2_grpc.py` (generated protobuf)
- **CI**: all green (Pre-commit, CI, CodeQL, OpenSSF Scorecard)

### 3. Tokenizer Fix
- `src/director_ai/core/scoring/nli.py:150` — `use_fast=False`
- `src/director_ai/core/scoring/scorer.py:304` — `use_fast=False`
- Fixes `RuntimeError: Already borrowed` with tokenizers >=0.21

### 4. Training Data Pipeline Extension
- `training/data_pipeline.py` — added `_load_ragtruth()`, `_load_summac()`, `_load_aggrefact()`
- CLI: `--all`, `--include-ragtruth`, `--include-summac`, `--include-aggrefact`
- SummaC unavailable on HF Hub (graceful skip)
- Dataset rebuilt: **495,631 samples** (was 734K with uncapped VitaminC)
  - RAGTruth: 2,700
  - AggreFact (11 sub-datasets): 29,320
  - New source distribution in `training/data/stats.json`

### 5. Build Judge Dataset Extension
- `training/build_judge_dataset.py` — `--subsample 0` (all), `--borderline-keep 0`, multi-GPU
- `score_with_nli_multigpu()` — splits dataset into shards, parallel ProcessPoolExecutor
- `_score_gpu_shard()` — per-GPU worker with CUDA_VISIBLE_DEVICES isolation

### 6. ROCm Venv Setup
- `.venv/` on NTFS disk (root partition full at 824MB)
- PyTorch 2.5.1+rocm6.2, transformers 4.49.0, sentencepiece
- PIP_CACHE_DIR and TMPDIR redirected to NTFS

## In Progress

### NLI Scoring (PID 365835)
- **446K train samples** across 3 GPUs (GPU 2, 3, 4)
- GPU 2: ~16.7/s → ETA ~1.4h (fastest)
- GPU 3: ~10.6/s → ETA ~2.9h
- GPU 4: ~10.7/s → ETA ~2.8h
- Log: `/tmp/build_judge_full.log`
- Output: `training/data_judge/` (will be overwritten when done)

## Blocked / Needs Attention

### E2E Benchmark OOM on RX 6600 XT
- FactCG-DeBERTa-v3-Large (304M) + judge (184M) + vectorstore = OOM on 8GB
- Latency benchmark passes: **median 13.6ms** (comparable to L40S)
- E2E results exist from L40S (500 + 1000 samples) at `benchmarks/results/judge_bench_*.json`
- Options: FP16 inference, or accept L40S results as authoritative

### Judge Benchmark Diagnosis (from L40S results)
- **QA**: F1 88%, FPR 4.8% — excellent
- **Dialogue**: F1 67%, FPR 96% — catastrophic
- **Summarisation**: F1 66%, FPR 98% — catastrophic
- Root cause: training data lacks quality dialogue/summarisation examples
- Fix: expanded dataset with RAGTruth + AggreFact should help

## Next Steps

1. **Wait for NLI scoring to complete** (~2-3h)
2. **Retrain judge** on expanded borderline dataset (GPU 1 or GPU 2 when free)
3. **Re-benchmark** to verify dialogue/summarisation FPR improvement
4. **Version bump** to v3.12.0 if results improve significantly

## GPU Allocation
| GPU | Status | Task |
|-----|--------|------|
| 0 | Mining (99%) | — |
| 1 | **IDLE** | Available for work |
| 2 | NLI scoring shard 0 (98%) | ~1.4h remaining |
| 3 | NLI scoring shard 1 (99%) | ~2.9h remaining |
| 4 | NLI scoring shard 2 (76%) | ~2.8h remaining |

## Key File Paths
- Repo: `/media/anulum/724AA8E84AA8AA75/aaa_God_of_the_Math_Collection/03_CODE/DIRECTOR_AI/`
- Venv: `.venv/` (ROCm, on NTFS)
- Judge model: `training/output/deberta-v3-base-judge/`
- Base dataset: `training/data/` (495K samples)
- Judge dataset: `training/data_judge/` (being rebuilt)
- Benchmark results: `benchmarks/results/judge_bench_*.json`
- NLI scoring log: `/tmp/build_judge_full.log`
