# Direct dependencies for the distillation training container.
# Transitive closure is resolved via ``pip-compile --generate-hashes``
# into ``requirements-distil.txt``; the Dockerfile installs the
# generated file with ``pip install --require-hashes`` so every
# resolved package is locked by SHA-256.
#
# ONNX export is handled by the project exporter on the current
# torch/transformers stack. The training image stays pure PyTorch;
# graph/runtime export wheels are installed only in export-capable images.
torch==2.12.0
transformers==5.10.2
datasets==3.6.0
scikit-learn==1.6.1
accelerate==1.7.0
huggingface-hub==1.20.1
google-cloud-storage==3.12.0
sentencepiece==0.2.1
