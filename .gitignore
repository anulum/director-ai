# ─────────────────────────────────────────────────────────────────────
# Director-Class AI — .gitignore
# https://github.com/anulum/director-ai
# ─────────────────────────────────────────────────────────────────────

# =====================================================================
# LOCAL-ONLY DIRECTORIES (NEVER pushed to remote)
# =====================================================================

# Private drafts, ideas, and TODO notes
.local/
DEV_LOG.md
claude-director-ai.md
.claude/
.coordination/
.gemini_audit_prompt.md

# Outreach emails, scripts, logs (contains credentials and contact data)
docs/internal/
docs/superpowers/

# Internal audit/review documents (never public-facing)
SECURITY_AUDIT_PREP.md
AUDIT_*.md

# GPU training artifacts (local-only)
*.tar.gz
gpu_results/

# Documentation prose (quarantined until headers/protocols are reviewed)
# Remove this line once docs/ prose files have been audited.
# docs/assets/ is tracked (header image, generator script).
docs/*
!docs/assets/
!docs/conf.py
!docs/index.rst
!docs/api/
!docs/_static/
!docs/ROADMAP_STATUS.md
!docs/API_REFERENCE.md
!docs/DATA_PIPELINE.md
!docs/BUILD_JUDGE_DATASET.md
!docs/E2E_EVAL.md
!docs/JUDGE_BENCHMARK.md
!docs/RUST_FFI_PIPELINE.md
!docs/BENCHMARKS.md
!docs/PRODUCTION_CHECKLIST.md
!docs/SIEVING.md
!docs/_generated/
!docs/_generated/capability_manifest.json
!docs/_generated/capability_snapshot.md
docs/_build/

# =====================================================================
# Python
# =====================================================================
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# =====================================================================
# Virtual Environments
# =====================================================================
.venv
.venv/
venv/
env/
ENV/
env.bak/
venv.bak/

# =====================================================================
# Testing
# =====================================================================
.pytest_cache/
.hypothesis/
.coverage
htmlcov/
*.cover
.tox/
.nox/

# =====================================================================
# Type Checking / Linting
# =====================================================================
.mypy_cache/
.ruff_cache/
.dmypy.json
dmypy.json

# =====================================================================
# JavaScript / TypeScript
# =====================================================================
node_modules/

# =====================================================================
# IDE / Editor
# =====================================================================
.idea/
.vscode/
*.swp
*.swo
*~
*.sublime-project
*.sublime-workspace

# =====================================================================
# OS Artifacts
# =====================================================================
.DS_Store
Thumbs.db
Desktop.ini
ehthumbs.db

# =====================================================================
# Jupyter
# =====================================================================
.ipynb_checkpoints/

# =====================================================================
# Environment Variables & Secrets
# =====================================================================
.env
.env.local
.env.*.local
*.pem
*.key

# =====================================================================
# Logs
# =====================================================================
*.log
logs/

# =====================================================================
# ML Model Artifacts (large binaries)
# =====================================================================
*.pt
*.pth
*.onnx
*.safetensors
*.bin
!requirements*.txt

# =====================================================================
# Simulation / Benchmark Outputs
# =====================================================================
*.npz
*.pkl
!src/director_ai/models/*.pkl
!src/director_ai/core/models/*.pkl
# Pickle-free model artefact (replaces the legacy .pkl); must ship in the wheel.
!src/director_ai/core/models/*.json
*.pickle
benchmarks/results/*
# The orchestrator schema doc lives under benchmarks/results/ so
# every generated result is next to the contract it adheres to.
!benchmarks/results/SCHEMA.md
!benchmarks/results/medical_eval.json
!benchmarks/results/finance_eval.json
!benchmarks/results/legal_eval.json
!benchmarks/results/jailbreak_detection.json
benchmarks/.cache/
benchmarks/*.jsonl
benchmarks/heldout/
!benchmarks/multilingual_corpus.jsonl
!benchmarks/frontierfail_cases.jsonl
!benchmarks/pint_seed_cases.jsonl
benchmarks/forge_ab_results.json

# =====================================================================
# HuggingFace Model Cache & Training Artifacts
# =====================================================================
ckpts/
# Ignore everything under /training/ EXCEPT the whitelisted files
# below. Pattern is ``/training/*`` (not ``/training/``) so the
# directory itself is not excluded — required for ``!`` exceptions
# to take effect on child paths.
/training/*
!/training/__init__.py
!/training/_upload_all.py
!/training/build_judge_dataset.py
!/training/data_pipeline.py
!/training/build_contradiction_dataset.py
!/training/train_contradiction.py
!/training/eval_contradiction_holdout.py
!/training/distil_factcg.py
!/training/distil_v5_cloud.py
!/training/lite_scorer_v2_vertex_runner.py
!/training/train_distillation.py
!/training/Dockerfile.distil
!/training/Dockerfile.benchmarks
!/training/Dockerfile.lite_scorer_v2
!/training/cloudbuild.lite_scorer_v2.yaml
!/training/launch_cloud.sh
!/training/requirements-distil.in
!/training/requirements-distil.txt
!/training/run_vertex.sh
!/training/train_cloud_large.py
!/training/train_hallucination_detector.py
!/training/train_judge.py
!/training/upcloud_setup.sh
!/training/upload_to_hub.py
!/training/vertex_model_benchmark.py
data/
/models/
!src/director_ai/models/
features/
*.tmp

# MkDocs build output
site/

# Local backups — never committed
BACKUP/
ARCHIVE/
MODELS/

# Lower-case models symlink (machine-specific path on dev rig)
/models
# nohup wrapper output — ephemeral
nohup.out
# Auto-generated benchmark / analysis reports — never committed
/reports/

# Internal analysis reports
docs/internal/gemini/

# Multi-agent coordination metadata (local audit snapshot, never committed)
.agent_metadata.json
