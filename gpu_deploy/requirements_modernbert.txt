# Director-AI ModernBERT training dependencies
# Install after PyTorch+CUDA: pip install torch
# flash-attn installed separately (requires --no-build-isolation)
torch>=2.8,<3
transformers>=5.0.0rc3,<6
datasets>=2.14
scikit-learn>=1.3
numpy>=1.24
nltk>=3.9.4
accelerate>=0.24
sentencepiece
protobuf
